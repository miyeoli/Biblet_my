<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="userDAO">
 	
 	<select id="loginCheck" resultType="String">
  		SELECT * FROM member WHERE mem_id=#{mem_id} AND mem_pass=#{mem_pass}
 	</select>
 	
 	<!-- 인기 도서 -->
 	<select id="starbook" resultType="int">
 	SELECT bookshelf.isbn FROM bookshelf 
	INNER JOIN appraisal ON bookshelf.book_status_num = appraisal.book_status_num
	GROUP BY bookshelf.isbn ORDER BY AVG(appraisal.star) DESC DESC
 	</select>
 	
</mapper>