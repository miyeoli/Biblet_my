<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="memberDAO">
 	
 	<!-- 로그인 -->
 	<select id="selectById" resultType="memberVO">
  		SELECT * FROM member WHERE mem_id=#{mem_id} AND AUTHSTATUS = '1'
 	</select>
 	
 	<!-- admin로그인 -->
 	<select id="selectByAdmin" resultType="adminVO">
  		SELECT * FROM member WHERE adm_Id=#{adm_Id}
 	</select>
 	
 	<!-- 아이디 찾기 -->
 	<select id="findById" resultType="memberVO">
  		SELECT mem_id FROM member WHERE mem_email=#{mem_email}
 	</select>
 	
</mapper>